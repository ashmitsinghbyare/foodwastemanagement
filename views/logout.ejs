<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logout</title>

    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            text-align: center;
            padding: 50px;
        }

        h1 {
            font-size: 28px;
            color: #333;
        }

        .message {
            font-size: 18px;
            color: #555;
            margin-bottom: 20px;
        }

        .logout-btn-container {
            margin-top: 20px;
        }

        button {
            padding: 12px 25px;
            background-color: #0077b6;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            margin: 10px;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #19c1b6;
        }

        .home-btn {
            background-color: #ff7043;
        }

        .home-btn:hover {
            background-color: #ff5722;
        }

        .logout-btn-container i {
            margin-right: 10px;
        }

        .fade-in {
            animation: fadeIn 1.5s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
    </style>
</head>
<body>

    <!-- Main content of the logout page -->
    <div class="logout fade-in">
        <h1>You have been logged out successfully</h1>
        <p class="message">Thank you for using FoodSave! You can either log in again or return to the homepage.</p>

        <div class="logout-btn-container">
            <!-- Login Again button -->
            <button onclick="location.href='<%= loginUrl %>'">
                <i class="fas fa-sign-in-alt"></i> Log in again
            </button>

            <!-- Return to Home button -->
            <button class="home-btn" onclick="location.href='<%= homeUrl %>'">
                <i class="fas fa-home"></i> Return to Home
            </button>
        </div>
    </div>

</body>
</html>
