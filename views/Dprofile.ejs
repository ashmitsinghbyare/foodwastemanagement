<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile | Food Donate</title>
  <link rel="stylesheet" href="/css/profil.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta2/css/all.min.css">
</head>
<body>

  <%- include('partials/profileNavbar') %>

  <div class="profile">
    <p class="headingline"><i class="fa fa-user" style="margin-right:10px;"></i>Profile</p>

    <% if (user && user.role === 'donor') { %>
      <!-- Only render the profile for users with the 'donor' role -->
      <img src="<%= user.profileImage || '/uploads/default-avatar.jpg' %>" alt="Profile Image" class="profile-img" />
      <div class="info">
        <p><strong>Name:</strong> <%= user.firstName %> <%= user.lastName %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Gender:</strong> <%= user.gender %></p>
        <a class="edit-profile-btn" href="/deditprofile">Edit Profile</a>
        <a class="logout-btn" href="/logout">Logout</a>
      </div>
    <% } else { %>
      <!-- If user is not logged in or is not a donor -->
      <p>User not found or you do not have permission to view this page.</p>
    <% } %>

  </div>

</body>
</html>
