<nav class="nav-bar">
  <div><strong>🥗 FoodSave</strong></div>
  
  <!-- Display user image and name -->
  <div class="nav-user">
    <% if (user && user.role === 'donor') { %>
      <img src="<%= user.profileImage || '/uploads/default-avatar.jpg' %>" alt="User Avatar" class="user-avatar" />
      <span><%= user.name %></span>
    <% } else { %>
      <img src="/uploads/default-avatar.jpg" alt="Guest Avatar" class="user-avatar" />
      <span>Guest</span>
    <% } %>
  </div>
  
  <div class="nav-links">
    <a href="/dashboard">Dashboard</a>
    <a href="/food-donate">Donate</a>
    <a href="/history">History</a>
    <a href="/logout">Logout</a>
  </div>
</nav>


<style>
  .nav-bar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    background-color: #2c3e50;
    color: white;
  }

  .nav-links {
    display: flex;
    gap: 20px;
  }

  .nav-links a {
    text-decoration: none;
    color: white;
    font-size: 16px;
  }

  /* Add hover effect on links */
  .nav-links a:hover {
    color: #f39c12;
  }

  /* Style the user section */
  .nav-user {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  /* Style the user image */
  .user-avatar {
    width: 35px;
    height: 35px;
    border-radius: 50%;
    object-fit: cover; /* Ensures the image fits inside the circle */
  }

  .nav-user span {
    font-weight: bold;
  }
</style>
