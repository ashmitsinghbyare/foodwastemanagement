<nav class="nav-bar">
  <div class="nav-title">FoodSave Receiver
    <a class="Na" href="/home">Home</a>
    <a class="Na" href="/account">Settings</a>
    <a class="Na" href="/logout">Logout</a>
  </div>
  <div class="profile-dropdown">
    <!-- Profile Image -->
    <img src="<%= user && user.profileImage ? user.profileImage : '/uploads/default-avatar.jpg' %>" 
         alt="User Profile" width="45" height="45" style="border-radius: 50%;" />
    
    <!-- Display Name -->
    <span class="profile-name"><%= user && user.name ? user.name : 'Guest' %> ▼</span>
    
    <!-- Dropdown Menu -->
    <div class="dropdown-content">
      <a href="/home">Home</a>
      <a href="/Rprofile">Edit Profile</a>
      <a href="/account">Settings</a>
      <a href="/logout">Logout</a>
    </div>
  </div>
  
</nav>
<style>
  /* Base Navbar */
.nav-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: #1b4d2a; /* Deep green */
  padding: 12px 24px;
  color: white;
  font-family: 'Segoe UI', sans-serif;
}

/* Title and links */
.nav-title {
  font-size: 1.5em;
  font-weight: bold;
  color: #a8ffb1;
}

.nav-links {
  display: inline-block;
  margin-left: 20px;
}

.Na {
  margin: 0 12px;
  text-decoration: none;
  color: #e0ffe9;
  transition: color 0.3s ease;
}

.Na:hover {
  color: #75ff9b;
}

/* Profile dropdown */
.profile-dropdown {
  position: relative;
  display: flex;
  align-items: center;
  cursor: pointer;
}

.profile-img {
  width: 38px;
  height: 38px;
  border-radius: 50%;
  object-fit: cover;
  margin-right: 8px;
  border: 2px solid #a8ffb1;
}

.profile-name {
  color: #e6ffe8;
  font-weight: 500;
}

.dropdown-content {
  display: none;
  position: absolute;
  top: 48px;
  right: 0;
  background-color: #2e6f42;
  min-width: 160px;
  box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
  border-radius: 6px;
  z-index: 10;
}

.dropdown-content a {
  color: #e0ffe9;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
  transition: background-color 0.3s ease;
}

.dropdown-content a:hover {
  background-color: #3b9057;
}

/* Show dropdown on hover */
.profile-dropdown:hover .dropdown-content {
  display: block;
}




</style>
