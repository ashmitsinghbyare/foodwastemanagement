<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Donation History | FoodSave</title>
  <link rel="stylesheet" href="/css/history.css" />
</head>
<body>

  <!-- ✅ Include Navbar Partial -->
  <%- include('partials/historyNavbar') %>

  <!-- ✅ Main Content -->
  <div class="main-content">
    <h2>Donation History</h2>

    <% if (donationData.length === 0) { %>
      <p style="text-align: center; padding: 1rem;">No donations made yet.</p>
    <% } else { %>
      <table id="donationTable">
        <thead>
          <tr>
            <th>Date</th>
            <th>Type</th>
            <th>Quantity</th>
            <th>Expiry</th>
            <th>Pickup Slot</th> <!-- ✅ New Column -->
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          <% donationData.forEach(entry => { %>
            <tr>
              <td><%= entry.date %></td>
              <td><%= entry.type %></td>
              <td><%= entry.quantity %></td>
              <td><%= entry.expiry || 'N/A' %></td>
              <td><%= entry.pickupTime || 'Not set' %></td> <!-- ✅ New Data -->
              <td class="<%= getStatusClass(entry.status) %>"><%= entry.status %></td>
            </tr>
          <% }); %>
        </tbody>
        
      </table>
    <% } %>
  </div>

  <!-- ✅ Footer -->
  <div class="footer">
    &copy; 2025 FoodSave Initiative. All rights reserved.
  </div>

</body>
</html>
